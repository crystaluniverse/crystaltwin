apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crystaltwin.fullname" . }}-test-connection"
  labels:
    {{- include "crystaltwin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crystaltwin.fullname" . }}:{{ .Values.service.port }}']
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crystaltwin.fullname" . }}:{{ .Values.service2.port }}']
  restartPolicy: Never
